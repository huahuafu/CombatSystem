# 阶段 A：评估与复盘闭环（版本迭代）

## 目标

在不改交战裁决引擎的前提下，完成「数据可对齐、结论可读、会话可追溯」的复盘闭环。

## 交付项

| 项 | 说明 | 状态 |
|----|------|------|
| A1 综合复盘包导出 | 复盘中心一键导出 JSON：`roundArchive` + `/combat/stats` + `/combat/interaction-event/list` + 仿真 state，便于离线对齐后端事实 | 已完成 |
| A2 规则化简报 | 基于 `roundArchive` 的固定规则结论（非 LLM），展示于综合评估卡片区域 | 已完成 |
| A3 会话 / Run 关系说明 | 复盘页明确：会话内摘要来自仿真页；Commander `runs` 为独立持久化记录 | 已完成 |

## 后续阶段（不在 A）

- B：战后 AI 简报（结构化 schema + 降级模板）
- C：参谋 / 方案 AI 与杀伤链联动
- D：对阵 BOT
